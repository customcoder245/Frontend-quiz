# Professional React + Vite Setup

This project is an industrial-grade frontend setup built with **React**, **Vite**, **Tailwind CSS v4**, and **TypeScript**.

## 🚀 Features

- **Framework**: React 19 (Vite)
- **Styling**: Tailwind CSS v4 with a custom design system
- **Architecture**: Professional folder structure (Atomic Design adjacent)
- **Deployment**: Pre-configured for Vercel
- **Quality Control**: Prettier and ESLint integrated
- **Utilities**: `clsx` and `tailwind-merge` for robust class management

## 📁 Project Structure

- `src/components`:
  - `ui/`: Fundamental UI components (buttons, inputs, etc.)
  - `common/`: Shared business-logic components
  - `layout/`: Layout-specific components (Header, Footer)
- `src/hooks/`: Custom React hooks
- `src/lib/`: External library configurations
- `src/services/`: API and third-party service logic
- `src/types/`: TypeScript definitions
- `src/context/`: React Context providers
- `src/utils/`: Helper functions
- `src/styles/`: Global styles and Tailwind configuration

## 🛠️ Getting Started

1. **Install Dependencies**:
   ```bash
   npm install
   ```

2. **Run Development Server**:
   ```bash
   npm run dev
   ```

3. **Build for Production**:
   ```bash
   npm run build
   ```

## 🌐 Deployment

This project is optimized for [Vercel](https://vercel.com). Simply link your repository for automated CI/CD.

---

*Generated by Antigravity*
